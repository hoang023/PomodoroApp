<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pomodoro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pomodoro">
        <activity android:name=".SignIn.SignInActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUp.SignUpActivity" />
        <activity android:name=".Statistical.StatisticalActivity" />
        <activity android:name=".SetTime.SettimeActivity" />
        <activity android:name=".TasksFunctions.TasksActivity" />
        <activity android:name=".TasksFunctions.TasksUpdate" />
        <activity android:name=".TasksFunctions.TasksAdd" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>